import { Route } from "mentine";

export const getTypescriptInterfaces = (route: Route): string => {
  return "";
};

export const getSanitizers = (route: Route): string => {
  return "";
};

export const getValidators = (route: Route): string => {
  return "";
};
